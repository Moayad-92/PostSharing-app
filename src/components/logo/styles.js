import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  logo_container: {
    marginBottom: 20,
    alignItems: 'center',
  },
  logo_text: {
    fontSize: 20,
    fontWeight: 'bold',
    color: 'white',
  },
});
