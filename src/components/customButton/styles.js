import {StyleSheet, Dimensions} from 'react-native';

const styles = StyleSheet.create({
  button_container: {
    backgroundColor: 'tomato',
    padding: 8,
    margin: 20,
    borderRadius: 5,
  },
  button_text: {
    color: 'white',
    fontSize: 16,
    fontWeight: 'bold',
    letterSpacing: 1.5,
    textAlign: 'center',
  },
});

export {styles};
